apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: quarkus-demo
  labels:
    app: quarkus-demo
spec:
  to:
    kind: Service
    name: quarkus-demo
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
